apiVersion: v1
kind: Service
metadata:
  name: {{ include "apchart.fullname" . }}
  labels:
    {{- include "apchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apchart.selectorLabels" . | nindent 4 }}
